name: add_hosts_master
on:
  push:
    branches: [ master , developer, Feature ]
  watch:
    types: [started]
  schedule:
    - cron: '55 * * * *'

jobs:
 make_master:
 name: master
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        persist-credentials: false
    - name: Create local changes
      run: |
        sudo rm -rf /etc/localtime
        sudo ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
        sudo apt-get install -y dos2unix
        bash update_action.sh
    - name: Commit files
      run: |
        git pull
        git config --local user.email ${{ secrets.MYEMAIL }}
        git config --local user.name ${{ secrets.MYUSERNAME }}
        git add .
        git commit -m "hosts update" -a
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
        force : true

make_Feature:
 name: Feature
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@Feature
    - uses: actions/checkout@v3
      with:
        persist-credentials: false
    - name: Create local changes
      run: |
        sudo rm -rf /etc/localtime
        sudo ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
        sudo apt-get install -y dos2unix
        bash update_action.sh
    - name: Commit files
      run: |
        git pull
        git config --local user.email ${{ secrets.MYEMAIL }}
        git config --local user.name ${{ secrets.MYUSERNAME }}
        git add .
        git commit -m "hosts update" -a
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
        force : true

make_developer:
  name: developer
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@developer
    - uses: actions/checkout@v3
      with:
        persist-credentials: false
    - name: Create local changes
      run: |
        sudo rm -rf /etc/localtime
        sudo ln -s /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
        sudo apt-get install -y dos2unix
        bash update_action.sh
    - name: Commit files
      run: |
        git pull
        git config --local user.email ${{ secrets.MYEMAIL }}
        git config --local user.name ${{ secrets.MYUSERNAME }}
        git add .
        git commit -m "hosts update" -a
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
        force : true
        
